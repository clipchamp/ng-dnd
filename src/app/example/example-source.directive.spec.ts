import { ExampleSourceDirective } from './example-source.directive';

describe('ExampleSourceDirective', () => {
  it('should create an instance', () => {
    const directive = new ExampleSourceDirective();
    expect(directive).toBeTruthy();
  });
});
