import { ExampleTitleDirective } from './example-title.directive';

describe('ExampleTitleDirective', () => {
  it('should create an instance', () => {
    const directive = new ExampleTitleDirective();
    expect(directive).toBeTruthy();
  });
});
